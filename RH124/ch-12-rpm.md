```shell
rpm <-q, -f>, rpm2cpio, dnf <list, search, history>
```

- rpm, [query and verifying], [install, upgrade, remove]
- [name, version, relaese, arch], GPG, cpio archive
- DNF, YUM `ls -l /bin/ | grep yum`, [BaseOS, AppStream, Errata]
- `/etc/yum.repos.d/*.repo`, `/var/log/dnf.rpm.log`