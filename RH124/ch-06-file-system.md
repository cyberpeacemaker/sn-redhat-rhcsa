```shell
whoami, pwd, df
cd (- ~, -P)
ls (-i, -l, -a), tree

```
- File-system [boot, root, home, tmp, run, var, usr, dev, etc], mounted on
- [absolutem relative] path, [current, parent] folder, navigate, manuplate
