TODO: LAB review
```shell
chmod (-R) (who/what/which target) , chown (-R, :group)
stat -c "%a" filename, attr (-l), getfattr (-d), getfacl
tree -ug

```

- file type , {files, direcotry}[rwx, special execute], flag, permission priority
- TODO:[setuid, setgid, sticky bit]
- umask `/etc/login.def`, `/etc/bashrc`, `/etc/profile`, `/etc/profile.d`, [`.bash_profile`, `.bashrc`]



